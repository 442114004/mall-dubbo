<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
            http://www.springframework.org/schema/beans/spring-beans.xsd
            http://code.alibabatech.com/schema/dubbo
            http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

	<dubbo:application name="dubbo-sport-provider" />

	<dubbo:registry id="sport-service"  address="${zk.url}" check="false"/>

	<dubbo:protocol name="dubbo" dispatcher="all" threadpool="cached" threads="1500"/>

	<!-- 提供服务接口 -->


	<!--<dubbo:service registry="sport-service"
				   interface="com.fittime.sport.service.QuestionService"
				   ref="questionService"
				   loadbalance="leastactive"
				   retries="1"
				   executes="1500"
				   timeout="30000"/>-->
</beans>